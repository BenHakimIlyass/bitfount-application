import { styled } from "stitches";

const Button = styled("button", {
  padding: "16px 30px",
  backgroundColor: "$blue",
  color: "#fff",
  borderRadius: "50px",
  fontWeight: 600,
  width: "fit-content"
});
export default Button;
